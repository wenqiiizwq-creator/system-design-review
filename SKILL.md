---
name: system-design-review
description: >-
  从硬件资料包建立设计基线，审查并优化系统级方案。已有需求/方案时核对合理性、
  覆盖和冲突；缺失时从原理图、BOM、核心器件资料及工程师对话提取方案并交其确认。
  适用于原理图设计或审查之前的需求、架构、电源、接口与关键选型工作；不替代
  schematic-review 的逐引脚/网表级准出，也不签 PCB 或实测性能。
---

# 硬件系统级设计审查与意图采集 · V2

目标：**资料包 → 有证据的设计意图与方案 → 审查/优化 → 工程师确认的受控基线**。
原理图可以是输入，基线可供新图绘制或已有原理图审查。审查对象是需求与方案决策，
不因图纸已经存在而跳过本阶段，也不要求先做完整逐引脚审查才能提取方案。

## 入口与路由

资料包先读 [接入协议](references/package-intake.md)，按内容、模块、版本识别：

- **已有需求和方案**：规范化内容后进模式 B；方案自身也要审查，不能直接当正确判据。
- **只有需求**：模式 A 建候选方案，说明新增决策，再进 B。
- **缺少需求/方案**：模式 A 按 [方案提取协议](references/design-reconstruction.md)
  从现有设计提取当前实现、候选意图与未知项，再定向采访。
- **局部缺失**：按模块混合执行，复用已确认内容，不重问已有答案。

**模式 A：采集与成稿** —— 读 [design-capture.md](references/design-capture.md)。
先做能由资料完成的工作，交付可审核草稿，再问能改变架构方向的缺失信息。
**模式 B：方案审查与优化** —— 读 [review-checklist.md](references/review-checklist.md)，
按需求、模块与工况建立检查台账；需要取舍时读 [optimization.md](references/optimization.md)。
A 成稿必须进入 B；B 整改更新候选基线并复核受影响项。AI 自己成稿不等于已审查或已获确认。

## 共同纪律

1. 分开记录 **意图**（希望实现）、**现状**（资料实际实现）、**约束**（器件/平台允许）。
   资料有出处只证明该主张有来源，不证明主张正确。图纸缺功能不能反写成“需求不需要”。
2. 参数标事实/假设/待确认，证据标 A/B/C；典型值、估算、测试条件和全角保证分开。
   关键 C 证据只能得 INSUFFICIENT。工程师确认目标不能覆盖器件的物理边界。
3. 证据定位到文档身份、版本、页/表/章节或对话记录；核精确 MPN、封装、装配和工况。
   未读、读取失败、被引用但缺失的附件均须留痕，不默认为无要求。
4. 工程师裁决目标和取舍；规范支撑器件约束；对应版本工程资料支撑实现。
   参考板、历史意见、Wiki 和旧报告用于找线索，不能覆盖已确认的适用规范。
5. 保护已确认的保留/删除/不可改动基线。发现证据明确的冲突要报告；不得无依据重设计。
6. 每检查项记录 PASS/FAIL/INSUFFICIENT/NA、严重度、置信度、证据、工况、改法和复验。
   定级按后果与必需性；未知不当低风险，改了字段不等于闭环。
7. 优化必须比较收益和代价，保留原始资料，交付候选修订版。接受优化要记录版本和范围，
   不能删除原需求让结果“通过”。仅标号映射更新不触发无意义的重新审签。

## 交付与准出

持续维护 `Design_Baseline_<项目>.md`、`design-intent.json`，模式 B 输出
`Design_Review_<项目>.md`。格式见 [report-template.md](references/report-template.md)，
机器契约见 [design-intent-schema.md](references/design-intent-schema.md)。

分别报告资料完整性、意图确认、技术审查和方案准出。只有适用检查完成、关键缺证与
缺陷闭环、当前意图/决策获工程师确认、必需移交被接收，才能声明：
**“系统方案基线可用于原理图设计/审查。”** 未满足时给草稿与明确未决项，继续独立工作。
不得以“有条件冻结”掩盖尚未满足的前置条件。边界和交接见
[scope-boundary.md](references/scope-boundary.md)。

## 工具与执行限制

- `scripts/inventory_package.py <目录> --json <工作目录>/package-manifest.json`：文件身份/哈希盘点；
  不解析文档内容，结果保持 unread，路由仍由内容审读决定。
- `scripts/check_requirements.py design-intent.json`：需求字段、来源、覆盖/验收和未确认项。
- `scripts/check_power_tree.py design-intent.json --fail-on-warn`：逐轨预算、未知负载、依赖/时序。
- `scripts/validate_design.py design-intent.json --json design-gate.json`：v2 基线覆盖、证据、
  当前确认与移交门禁。只有此脚本可计算 `freeze_allowed`，也不能替代工程判断。

退出 0 表示所选机械策略通过，1 表示缺陷/缺证，2 表示文件读写失败。两个基础脚本
永不签方案准出；旧版输入需迁移后才可使用最终门禁。脚本不认证来源真伪，不自动求解
所有电路；效率、并发峰值、浪涌、控制失效状态须形成专家预算与检查证据。
