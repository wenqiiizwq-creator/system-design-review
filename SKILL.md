---
name: system-design-review
description: >-
  审查硬件系统级设计方案（整体架构、电源树、外设分配、功能方案、关键选型），
  或通过自然语言对话采集设计意图并生成结构化硬件设计文档时使用。覆盖需求覆盖
  矩阵、架构/电源树/外设审计、方案级验算与 datasheet/平台规则符合性，签
  “系统方案可冻结并进入原理图设计”。不用于已绘制原理图的网表级审查
  （用 schematic-review），也不做 PCB 布局、SI/PI、EMC、热、DFM 或投板签核。
---

# 硬件系统级方案设计审查（方案设计 Agent）

定位：**原理图设计之前的质量门**。上游吃需求与对话，下游产出可进入原理图绘制的
设计基线。审查对象是文档与决策，不是网表。

## 双模式路由

- **模式 A：设计采集与成稿** —— 工程师用自然语言描述板卡，skill 分层采访、
  翻译成专业设计语言、增量成稿。触发词：生成/整理/起草设计文档、把对话转成
  方案、板卡需求不清晰。读 `references/design-capture.md`。
- **模式 B：系统级方案审查** —— 对已有方案（AI 生成或工程师手写）跑完整检查表。
  触发词：审查/评审方案、硬件架构、电源树、外设分配。读
  `references/review-checklist.md`。

两模式强制闭环：模式 A 成稿后必须跑模式 B；模式 B 的整改回到对话或文档修订。
**AI 自己生成的基线不算“已审查”**。

## 准出对象

只签“**系统方案可冻结并进入原理图设计**”。不签原理图（schematic-review 的职责），
更不签 PCB。边界矩阵与准出用语见 `references/scope-boundary.md`。

## 通用纪律（两模式共用）

1. **事实 / 假设 / 待确认三分**：有出处的参数标“事实”（文件:页码 或 对话轮次）；
   AI 推断的标“假设”并写推导依据；无来源的保留 `[XXX]` 标“待确认”，绝不默认为真。
2. **证据链**：datasheet 引用必须带文档名 + 版本 + 页码/表号；对话决策标轮次；
   平台规则标文件路径+章节。没有证据链的结论不进基线。
3. **四态结果 + 置信度**：每检查项 PASS / FAIL / INSUFFICIENT / NA；证据置信度
   A（官方文件已核）/ B（单一来源合理）/ C（证据不足）。C 级不得推出 A/B 结论，
   不确定性只能向下传播。
4. **定级**：BLOCKER（改架构方向或上电即坏）/ Warning（功能风险、违反强制条款）/
   Info（建议）/ 观察（需设计方确认）。
5. **闭环铁律**：“回复已修改”不等于闭环，必须对文档/参数重新验证。
6. **AI 也会算错**：算出的功耗、裕量、分压必须与至少一个独立迹象对撞（轨名语义、
   参考设计、同类平台），对不上先怀疑自己的计算。

## 交付物

- 模式 A 增量维护：`Design_Baseline_<项目>.md`（人读基线）+
  `design-intent.json`（结构化，供脚本校验）。模板与契约见
  `references/report-template.md`、`references/design-intent-schema.md`。
- 模式 B 输出：`Design_Review_<项目>.md`——逐检查项结果 + 需求覆盖矩阵 +
  【原】→【改】→【理】红线 + 待确认清单 + HANDOFF 移交约束。
- 下游衔接：冻结后的基线是绘制阶段唯一事实源；细化成 easyeda-schematic-draw
  的 Hardware_Design 契约时不得引入新决策，任何改动必须回本 skill 闭环。

## 确定性校验脚本

- `scripts/check_requirements.py design-intent.json`：需求覆盖闭合检查——每条需求
  必须有方案落点 + 验证方式；冲突、缺口、未确认项列出。
- `scripts/check_power_tree.py design-intent.json`：电源树逐轨求和与裕量校验——
  缺源/缺负载/缺电流报 FAIL，负载合计超额定报 FAIL，裕量 <20% 报 WARN。

脚本只做机械可穷举的检查；架构判断、功能链路走查、datasheet 条款符合性仍是
模式 B 的专家工作，脚本 0 错误 ≠ 方案通过。

## 高频错误模式库（每轮强制自查）

- [ ] 估算值当事实（电流/功耗/温度无出处即写入基线）
- [ ] 工程师没说就默默默认（成本/认证/供应商锁定/EMC 历史）
- [ ] 需求没落到方案章节就宣布“已覆盖”
- [ ] 电源轨缺“谁驱动/谁负载/多少电流”之一就通过
- [ ] 引脚复用/总线冲突只看名字不看电平域
- [ ] datasheet 强制条款被参考设计或历史板带偏
- [ ] 成稿后不跑模式 B 就进入绘制
- [ ] 对话中断后不写状态摘要，续不上进度
